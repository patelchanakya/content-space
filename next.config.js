/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "images.unsplash.com", "www.youtube.com", "m.youtube.com"],
    },
};

module.exports = nextConfig
